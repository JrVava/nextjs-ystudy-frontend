import React from "react";
import UniversityRoutes from "@/pages/guides/UniversityRoutes";

export const metadata = {
  title: "YStudy — Which route into university fits you",
  description: "Compare foundation, HND, top-up, degree and master routes in plain English for mature learners.",
};

export default function Page() {
  return <UniversityRoutes />;
}
