import React from "react";
import CareersSalaries from "@/pages/guides/CareersSalaries";

export const metadata = {
  title: "YStudy — What careers could your degree lead to",
  description: "Compare subject routes, salary ranges and career outcomes before choosing your degree.",
};

export default function Page() {
  return <CareersSalaries />;
}
