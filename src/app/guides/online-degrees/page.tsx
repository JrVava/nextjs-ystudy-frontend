import React from "react";
import OnlineDegrees from "@/pages/guides/OnlineDegrees";

export const metadata = {
  title: "Online and blended degrees",
  description: "Compare online, blended, weekend and campus study patterns before choosing your route.",
};

export default function Page() {
  return <OnlineDegrees />;
}
