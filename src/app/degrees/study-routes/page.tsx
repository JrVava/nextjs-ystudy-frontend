import StudyRoutes from "@/pages/degrees/StudyRoutes";

export const metadata = {
  title: "YStudy — Study Routes",
  description: "Compare higher education qualification routes in the UK. Find the right option between Foundation Year, CertHE, HND, and Bachelor's degrees.",
};

export default function Page() {
  return <StudyRoutes />;
}
