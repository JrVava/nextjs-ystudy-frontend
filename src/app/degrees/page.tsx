import Degrees from "@/pages/Degrees";

export const metadata = {
  title: "YStudy — Degrees",
  description: "Browse and compare flexible, SFE-eligible university degree courses built for adult learners.",
};

export default function Page() {
  return <Degrees />;
}
