import Subjects from "@/pages/degrees/Subjects";

export const metadata = {
  title: "YStudy — Study Subjects",
  description: "Explore flexible degree subjects for adult learners. Compare business, computing, health, law and construction routes.",
};

export default function Page() {
  return <Subjects />;
}
